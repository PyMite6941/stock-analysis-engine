fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
httpx>=0.27
# data providers
yfinance>=0.2.40   # default no-key provider
requests>=2.31     # used by the finnhub provider
# testing
pytest>=8.0
# spreadsheet export/import (CSV works without it; XLSX needs it)
openpyxl>=3.1
python-multipart>=0.0.9   # multipart parsing for the holdings file upload
