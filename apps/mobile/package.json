{
  "name": "stock-analysis-engine-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "iOS and Android shells around the web build (Capacitor).",
  "scripts": {
    "build:web": "cd ../../frontend && npm install && npm run build -- --mode app",
    "sync": "npm run build:web && cap sync",
    "open:android": "cap open android",
    "open:ios": "cap open ios",
    "run:android": "npm run sync && cap run android",
    "apk": "npm run sync && cd android && gradlew.bat assembleDebug"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  }
}
