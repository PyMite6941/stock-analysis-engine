# Dependencies for the Vercel @vercel/python serverless function (api/index.py).
# Deliberately lean: no uvicorn (Vercel provides the server), no yfinance
# (use DATA_PROVIDER=finnhub in production — yfinance is flaky from cloud IPs).
fastapi>=0.115
pydantic>=2.7
httpx>=0.27
requests>=2.31
